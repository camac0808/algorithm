// Recursion 재귀
// 1. 재귀란?
// - 함수가 자기 자신을 호출하는 것

function countDown(num) {
  // base case
  if (num <= 0) {
    console.log('Done!');
    return;
  }

  // recursive case
  console.log(num);
  num--;
  countDown(num);
}

countDown(5);